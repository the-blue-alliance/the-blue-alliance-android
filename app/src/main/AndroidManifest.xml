<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- DataExtractionRules: android:allowBackup is deprecated once minSdk >= 31, which only
         the metavr flavor is (32, Meta's release floor) — the gms flavor still ships to
         API 26 devices where the attribute is load-bearing. Splitting backup config into
         per-variant extraction rules is a product decision, not part of the Horizon
         packaging work; suppressed here until it's made deliberately. -->
    <application
        android:name=".TBAApplication"
        android:allowBackup="true"
        tools:ignore="DataExtractionRules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.TBA">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.TBA">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />


                <data android:scheme="https" />
                <data android:host="www.thebluealliance.com" />
                <data android:host="thebluealliance.com" />

                <data android:pathPrefix="/event/" />
                <data android:pathPrefix="/events" />
                <data android:pathPrefix="/match/" />
                <data android:pathPrefix="/team/" />
                <data android:pathPrefix="/teams" />
                <data android:pathPrefix="/regional_advancement" />
            </intent-filter>
        </activity>

        <!-- This alias insulates our shortcuts (which maintain the same Intent data through app updates)
            from potential future changes to our main launcher activity -->
        <activity-alias
            android:name="com.thebluealliance.android.LaunchShortcutActivity"
            android:exported="true"
            android:targetActivity=".MainActivity" />

        <meta-data
            android:name="google_analytics_automatic_screen_reporting_enabled"
            android:value="false" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
